# Python implementation plans

Design notes and plans that apply only to the Python DQL implementation in [`py-impl/`](../py-impl/).

Rust rewrite and parity work lives in [`rust-plans/`](../rust-plans/).
