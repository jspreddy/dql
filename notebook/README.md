# Notebook frontend

This folder will hold a JupyterLab install and DQL kernel config so `dql` (Python) and `dqlrs` (Rust) can be used from a notebook UI started on the CLI.

**Implementation is not in the tree yet.** The proposal is [`PLAN.md`](PLAN.md). Review that first.
