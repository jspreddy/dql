# pk / sk records

1000 JSON-line rows (10 attributes) generated by `generate_records.py` (Faker,
seed 42). Used by:

- `cases/200-select-pk-sk-filters/`
- `cases/300-update-except-pk-sk-filters/`

```bash
python3 -m pip install faker
python3 generate_records.py
```
