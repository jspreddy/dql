# pk / sk records

1000 JSON-line rows (10 attributes) generated by `generate_records.py` (Faker,
seed 42). Used by:

- `tests/test_200_select_pk_sk_filters.py`
- `tests/test_300_update_except_pk_sk_filters.py`

The SELECT oracle for the planted `acct-00` / `item#00` / active / us-west rows
is [`select_expected.json`](select_expected.json).

```bash
python3 -m pip install faker
python3 generate_records.py
```
